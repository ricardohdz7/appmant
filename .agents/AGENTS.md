# Reglas del Proyecto

- **Despliegues y Git Push**: No realices `git push` de forma automática tras hacer cambios en el código. Deja los cambios únicamente en el entorno local (`localhost`) para que puedan ser probados primero. Solo sube los cambios a GitHub (`git push`) cuando se solicite de forma explícita.
